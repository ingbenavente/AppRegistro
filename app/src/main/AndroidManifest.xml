<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appregistro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppRegistro">

        <!-- MainActivity con intent-filter -->
        <activity
            android:name=".Controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegistroActivity -->
        <activity
            android:name=".Controller.RegistroActivity"
            android:exported="false" />

        <!-- ListaActivity -->
        <activity
            android:name=".Controller.ListaActivity"
            android:exported="false" />

        <activity android:name=".Controller.DetalleActivity" />


    </application>

</manifest>
